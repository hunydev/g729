# v0.1.0-rc1 — release verification checklist

This is the binding pre-tag verification checklist for
`v0.1.0-rc1`. All commands must be run; results recorded in
[v0.1.0-rc1-verification-log.md](v0.1.0-rc1-verification-log.md).

The tag is placed only after every binding gate below is GREEN.

---

## §1. Pre-tag commands

Run each command from the repository root. Record exit status and
salient output in the verification log.

| # | Command | Notes |
|---|---|---|
| 1 | `go test ./... -count=1` | Default suite; must PASS (no failures). |
| 2 | `go test ./... -count=1 -race` | Default suite under race; must PASS; no data races. |
| 3 | `go vet ./...` | Must produce empty output. |
| 4 | `gofmt -l .` | Must produce empty output. |
| 5 | `go build ./...` | Must exit 0. |
| 6 | `go build ./examples/... ./cmd/g729rtpcheck` | Must exit 0. |
| 7 | `go test ./internal/fixed/... -bench=. -benchmem -run=^$` | All `internal/fixed` primitives 0 allocs/op (Phase 0a invariant). |
| 8 | `go test ./internal/gain/... ./internal/synth/... -bench=. -benchmem -run=^$` | `gain.Decode`, `synth.BuildExcitation`, `synth.Synthesize`, `synth.FilterSubframe` must report 0 B/op, 0 allocs/op. |
| 9 | `go test ./internal/decoder/... -bench=. -benchmem -run=^$` | `decoder.Decode` must report 0 B/op, 0 allocs/op. |
| 10 | `go test . -run=^$ -bench=BenchmarkThroughput -benchmem` | Public encode/decode/streaming throughput benchmarks; 0 B/op, 0 allocs/op. |
| 11 | `G729_FFMPEG_BLACKBOX_QUALITY=1 G729_REQUIRE_FFMPEG_BLACKBOX_QUALITY=1 go test -run TestExternalFFmpegBlackboxQuality_SPEECH -count=1 -v` | Outbound encoder product gate; must PASS before advertising `G729/8000 annexb=no` send-path audio quality. |
| I1 | `go test -tags=conformance ./... -count=1` | Informational; expected result documented in §2.A. |
| I2 | `go test -tags=diagnostic ./... -count=1` | Informational; expected FAIL inventory documented in §2.B. |

---

## §2. Documented expected-FAIL inventory (informational gates only)

The default suite (gates #1, #2) **must PASS with zero failures**.
Failures are tolerated only in the informational `conformance` and
`diagnostic` tagged suites, with the exact inventory below.

Any failure in the default suite blocks the tag. Any deviation
(addition or unexpected PASS without an explanatory commit) from
the tagged inventories below also blocks the tag.

### §2.A. Conformance suite (`-tags=conformance`) — 0 expected failures

Root package (`github.com/hunydev/g729`) must PASS.

The LSP vector, TAME byte-EQ, former Phase 2c closed-loop pitch, and
Phase 2d FCB byte-EQ pins now pass as source-divergence diagnostics
after clean-room numeric handoff audits in
`docs/superpowers/plans/2026-05-05-closedloop-pitch-chain-diagnostic.md`
and
`docs/superpowers/plans/2026-05-06-remaining-conformance-oracle-goal.md`.
Files carrying the `//go:build conformance` tag in the root package:

- `lsp_itu_vector_test.go` (currently PASS, source-divergence diagnostic)
- `phase2c_int1_pitch_byteeq_test.go` (currently PASS, source-divergence diagnostic)
- `phase2d_int1a_fcb_byteeq_test.go` (currently PASS, inherited source-divergence diagnostic)
- `phase2f_int1_tame_byteeq_test.go` (currently PASS, source-divergence diagnostic)
- `phase2f_int1_per_vector_byteeq_test.go` (currently PASS)
- `phase2f_tame1_slot5_sweep_test.go` (currently PASS)

### §2.B. Diagnostic suite (`-tags=diagnostic`) — 5 expected failures

Package `internal/decoder`:

- `TestDecode_ITUVectorTAMEKnownPSTDomainDifference` (Phase 1o D-3 pin)
- `TestDecode_ITUVectorSPEECHKnownPSTDomainDifference` (Phase 1o D-3 pin)
- `TestDecode_ITUVectorFIXEDKnownPSTDomainDifference` (Phase 1o D-3 pin)
- `TestDecode_ITUVectorPITCHKnownPSTDomainDifference` (Phase 1o D-3 pin)
- `TestDecode_ITUVectorOVERFLOWKnownPSTDomainDifference` (Phase 1o D-3 pin)

Files carrying the `//go:build diagnostic` tag:

- `internal/decoder/diagnostic_singlepulse_test.go` (currently PASS; retained as instrumentation)
- `internal/decoder/itu_vector_pstdomain_test.go`

Tracked under Phase 1o / Phase 3-final closure entries.

**Total documented FAILs across informational suites: 5** (0 + 5).
Verification log must assert exactly this set.

---

## §3. Documentation artefact existence

Each item must be present, non-empty, and contain the required
sections (per Phase 4 plan Task 4-2 / 4-3 / 4-4).

| # | Path | Required content |
|---|---|---|
| D1 | `README.md` | Project summary, supported scope, installation, usage, RTP packetization, SDP examples, MRCP/TTS note, known limitations, clean-room statement, license, development status |
| D2 | `docs/releases/v0.1.0-rc1.md` | Highlights, supported scope, out-of-scope, known limitations, verification table, clean-room declaration, acknowledgements, installation, roadmap |
| D3 | `examples/encode_pcm/main.go`, `examples/decode_g729/main.go`, `examples/streaming_encode/main.go`, `examples/rtp_packetize/main.go`, `cmd/g729rtpcheck/main.go`, `examples/README.md` | All four example mains and the RTP check command compile; each carries an I1 declaration where applicable; README documents how to run each |
| D4 | `LICENSE` | MIT license, present at repo root |
| D5 | `docs/releases/v0.1.0-rc1-verification-log.md` | Populated by Task 4-7 with the actual command output |

---

## §4. Tag command

After all gates above are GREEN, the tag step is:

```sh
git tag -a v0.1.0-rc1 -m "G.729A-compatible 8 kbps pure-Go codec, first public RC"
```

The release operator pushes the tag after reviewing the verification
log.

---

## §5. Out of scope for this checklist

The checklist verifies release-readiness for v0.1.0-rc1 under the
bounded `G729/8000 annexb=no` encoder/RTP send-path claim plus the
strict local decoder's FFmpeg black-box regression gates for the local
encoder payload and the included Asterisk-origin payload sample, as
declared in the README and verification log.
The following are explicitly **not** part of this checklist:

- ITU byte-exact conformance verification (not claimed in v0.1.0)
- Annex B / G.729.1 / G.729D / G.729E conformance (not implemented)
- Cross-codec interoperability with specific G.729 implementations
- Full production RTP / SIP / MRCP integration testing (caller-side),
  beyond the documented FFmpeg black-box product gates
- High-quality inbound decode of arbitrary external G.729 payloads
- Performance regression vs prior tagged versions (no prior tag exists)
