# v0.1.0 Verification Log

Date: 2026-05-16
Release tag: `v0.1.0`

This log records the local engineering gates used before cutting the official
`v0.1.0` tag. It is not a certification or endorsement claim.

## Commit

| Field | Value |
|---|---|
| Release commit | Commit pointed to by tag `v0.1.0` |
| Worktree status | Clean before tag |

## Required Gates

| Gate | Result | Notes |
|---|---|---|
| Default tests | Pass | `env GOCACHE=/tmp/go-build go test ./... -count=1` |
| Race tests | Pass | `env GOCACHE=/tmp/go-build go test ./... -race -count=1`; first attempt hit local `/tmp` space exhaustion, rerun passed after clearing Go build cache. |
| Vet | Pass | `env GOCACHE=/tmp/go-build go vet ./...` |
| Format check | Pass | `gofmt -l .` produced no output. |
| Build all packages | Pass | `env GOCACHE=/tmp/go-build go build ./...` |
| Build examples/tools | Pass | `env GOCACHE=/tmp/go-build go build ./examples/... ./cmd/g729rtpcheck ./cmd/g729loadtest` |
| Distribution hygiene | Pass | `env GOCACHE=/tmp/go-build go test ./... -run 'TestMITDistributionAudit|TestOracleHandoff_' -count=1` |

## Performance Gates

| Gate | Result | Summary |
|---|---|---|
| Fixed-point microbenchmarks | Pass | `Add 0.2694 ns/op`, `LMult 0.3456 ns/op`, `LMac 0.5494 ns/op`, `DivS 5.026 ns/op`, `NormL 8.648 ns/op`; all `0 B/op`, `0 allocs/op`. |
| Gain/synthesis microbenchmarks | Pass | `gain.Decode 129.8 ns/op`; `BuildExcitation 239.0 ns/op`; `Synthesize 826.5 ns/op`; `FilterSubframe 598.2 ns/op`; all `0 B/op`, `0 allocs/op`. |
| Decoder microbenchmarks | Pass | `BenchmarkDecode 12391 ns/op`, `0 B/op`, `0 allocs/op`. |
| Public throughput benchmark | Pass | Core encode `106195 ns/frame`, `0.01062 rtf`, `94.17 streams/core`; CoreFast encode `101639 ns/frame`, `0.01016 rtf`, `98.39 streams/core`; decode `11874 ns/frame`, `0.001187 rtf`, `842.2 streams/core`. |
| Single-stream encoder soak | Pass | 60s, 6000 frames, errors 0, `codec_rtf=0.028787`, `codec_streams_per_core=34.74`, deadline misses 0, p99 processing `397.14 us`, p99 deadline ratio `0.039714`. |
| Multi-stream loopback soak | Pass | 60s, 32 streams, 191997 frames, errors 0, `codec_rtf=0.016528`, `codec_streams_per_core=60.50`, deadline misses 3 (`0.000016`), p99 processing `265.05 us`, p99 deadline ratio `0.026505`. |

## Conditional Local Gates

| Gate | Result | Notes |
|---|---|---|
| FFmpeg outbound quality | Pass | SPEECH corpus, 3750 frames. `our encoder -> ffmpeg`: GlobalSNR `7.47 dB`, SegSNR `4.72 dB`; encoder-only delta vs FFmpeg reference decode: GlobalSNR `+0.43 dB`, SegSNR `+0.32 dB`. |
| Local decoder vs FFmpeg quality | Pass | SPEECH corpus, 3750 frames. `our encoder -> local decoder`: GlobalSNR `7.61 dB`, SegSNR `4.83 dB`; decoder delta vs FFmpeg path: GlobalSNR `+0.13 dB`, SegSNR `+0.11 dB`. |
| Private final PCM oracle | Pass | Private verifier output outside repo. Total exact `740800/740800`, mismatches 0, max_abs 0 across ALGTHM, ERASURE, FIXED, LSP, OVERFLOW, PARITY, PITCH, SPEECH, TAME, TEST. |

## Informational Gates

| Gate | Result | Notes |
|---|---|---|
| Conformance build tag suite | Pass | `env GOCACHE=/tmp/go-build go test -tags=conformance ./... -count=1` |
| Diagnostic build tag suite | Pass | `env GOCACHE=/tmp/go-build go test -tags=diagnostic ./... -count=1`; historical PST-domain drift monitors now pass when the current strict decoder path matches PST exactly. |

## Distribution Audit

| Check | Result |
|---|---|
| Clean worktree before tag | Pass |
| No private oracle artifacts in `git ls-files` | Pass |
| No third-party G.729 source or binaries in `git ls-files` | Pass |
| Ignored local private materials remain untracked | Pass |
